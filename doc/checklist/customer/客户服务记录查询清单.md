# 客户服务记录查询清单

## 业务域
- 客户

## 目标
- 将客户端“我的工单”和“咨询历史”两类历史查询能力补齐为明确可执行口径，覆盖列表、筛选、搜索、详情和数据落点。

## 开发任务
- [x] 在 `3.4.5 客户端我的工单` 中补充我的工单入口、列表、状态筛选、时间筛选、关键词搜索与详情可见范围。
- [x] 在 `3.5.4 客户端咨询历史` 中补充咨询历史入口、列表、状态筛选、时间筛选与详情回看规则。
- [x] 在 `ticket` 数据模型中补充 `created_at`、`updated_at`，支撑客户端按时间筛选与默认排序。
- [x] 在 `3.5.3 数据与安全` 中补充 WebSocket 长连接不写死单机上限、支持水平扩展的口径。
- [x] 在 `§7` 验收标准中增加客户端工单历史与咨询历史两项覆盖。

## 验证记录
- [x] 客户端能按当前账号查看自己的工单列表与详情。
- [x] 客户端能按当前账号查看自己的咨询历史列表与详情。
- [x] WebSocket 容量口径已从“固定数值”收口为“水平扩展 + 压测定容”。
