# 知识分享有效期对齐清单

## 业务域
- 知识库

## 目标
- 将知识条目的临时分享有效期与数据模型字段对齐，避免流程定义与实体字段不一致。

## 开发任务
- [x] 为 `article_share_log` 补充 `expires_at` 字段，用于临时分享过期时间。
- [x] 在 `3.6.5 库内引用` 中明确临时分享默认 15 天，并写入 `article_share_log.expires_at`。
- [x] 复查 `article_share_log` 与临时分享流程口径，确认字段与需求一致。

## 验证记录
- [x] `article_share_log` 已包含 `expires_at`。
- [x] 临时分享默认 15 天的口径已与实体字段对齐。
