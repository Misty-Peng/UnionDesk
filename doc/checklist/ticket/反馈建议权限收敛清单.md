# 反馈建议权限收敛清单

## 业务域
- 工单 / 反馈建议

## 目标
- 将反馈/建议的权限口径与工单体系收敛为同一套 `ticket:*` 权限项，避免额外定义 `feedback:*` 权限。

## 开发任务
- [x] 在 `3.7 反馈/建议` 中明确复用 `ticket:*` 权限项，不单独定义 `feedback:*`。
- [x] 保留反馈/建议独立提交入口，但不拆出新的权限模型。
- [x] 复查 `2.1.5 权限项表`，确认无需新增反馈/建议专属权限项。

## 验证记录
- [x] PRD 已明确反馈/建议复用工单权限项。
- [x] PRD 未新增 `feedback:*` 权限项。
