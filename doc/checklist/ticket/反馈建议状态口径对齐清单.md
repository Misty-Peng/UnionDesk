# 反馈建议状态口径对齐清单

## 业务域
- 工单 / 反馈建议

## 目标
- 统一反馈/建议的客户侧状态展示与内部处理结论，避免“已处理”与“已采纳/已拒绝”口径混淆。

## 开发任务
- [x] 在 `3.7 反馈/建议` 中明确客户侧仅展示“已收到 / 处理中”。
- [x] 明确内部仍按 `待评估 → 处理中 → 已处理` 流转。
- [x] 在 `5. 数据模型（核心实体）` 中补充 `ticket.result` 作为内部结论字段。
- [x] 复查反馈/建议口径，确认不向客户暴露 `adopted / rejected` 结论。

## 验证记录
- [x] 客户端不会显示“已采纳 / 已拒绝”。
- [x] 内部可记录反馈/建议的采纳结论。
- [x] `ticket.result` 仅用于反馈/建议内部结论。
