# 工单优先级规则对齐清单

## 业务域
- 工单

## 目标
- 明确客户提交工单时的优先级展示与默认值口径，避免客户端暴露不必要的调度维度。

## 开发任务
- [x] 在 `3.4.1 工单类型与动态表单` 中明确客户提交时不提供优先级选择。
- [x] 明确工单优先级由系统按工单类型和业务域规则默认赋值，客服/管理员可后续调整。
- [x] 在 `5. 数据模型（核心实体）` 中补充 `ticket.priority`。
- [x] 复查优先级口径，确认“紧急工单”仍由客服/管理员单独标记。

## 验证记录
- [x] 客户端提交表单不出现优先级字段。
- [x] 工单详情和队列可展示并筛选优先级。
- [x] 客服可在处理过程中调整优先级。
