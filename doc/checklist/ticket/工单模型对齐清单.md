# 工单模型对齐清单

## 业务域
- 工单

## 目标
- 将 PRD 中工单处理流程与工单数据模型对齐，避免流程要求与实体字段缺失。

## 开发任务
- [x] 为 `ticket` 核心字段补充 `version`，用于乐观锁校验。
- [x] 在 `3.4.3 工单处理流程` 中明确 `version` 的提交冲突提示口径。
- [x] 复查 `ticket` 实体字段列表，确认不再遗漏与并发控制直接相关的核心字段。

## 验证记录
- [x] `ticket` 数据模型已包含 `version`。
- [x] 工单详情提交冲突的文案与 `version` 字段绑定。
