# 工单编号规则对齐清单

## 业务域
- 工单

## 目标
- 明确工单编号生成规则、域前缀来源与数据库字段归属，避免编号口径不一致。

## 开发任务
- [x] 在 `3.4 工单系统` 中明确工单编号 `ticket_no` 的格式与生成时机。
- [x] 明确工单前缀复用 `business_domain.code`，并定义为稳定短码。
- [x] 在 `5. 数据模型（核心实体）` 中补充 `ticket_no` 字段。
- [x] 复查工单编号口径，确认全局唯一且不随域名称变更自动重算。

## 验证记录
- [x] 每张工单都有唯一 `ticket_no`。
- [x] 域前缀可稳定复用，不因业务域改名而变化。
- [x] `ticket_no` 可按前缀+日期+序号解析。
