# 反馈建议模块补全清单

## 业务域
- 工单 / 反馈建议

## 目标
- 将反馈/建议从“独立入口但仅三行描述”补全为可提交、可列表、可处理、可补充、可追踪 SLA 的完整闭环，并明确复用工单底座的方式。

## 开发任务
- [x] 在 `3.7 反馈/建议` 中补充客户提交入口、表单字段、客户列表与详情、管理端处理流、补充评论和 SLA 口径。
- [x] 在 `5. 数据模型` 中补充 `ticket_type`、`ticket`、`ticket_reply` 的反馈/建议复用说明。
- [x] 在 `7. MVP 验收标准` 中补充反馈/建议独立入口、管理端处理、追加评论和 SLA 验收项。
- [x] 明确反馈/建议沿用工单体系与 `ticket:*` 权限，不单独定义 `feedback:*` 权限。
- [x] 明确客户侧“我的反馈”入口、筛选、搜索与详情展示方式。
- [x] 明确客户补充说明的时间窗口与处理后不可追加边界。

## 验证记录
- [x] 已核对反馈/建议实际复用工单底座承载，不新增独立业务实体表。
- [x] 已确认反馈/建议可由专用 `ticket_type` 承载，并与工单类型体系保持一致。
- [x] 已确认管理端可对反馈进行筛选、搜索、领取、指派、备注、回复、关闭与导出。
- [x] 已确认反馈/建议复用工单 SLA 引擎，违约动作与工单保持一致。
- [x] 已确认客户侧仅展示已收到 / 处理中 / 已完成，不暴露 adopted / rejected 结论。
