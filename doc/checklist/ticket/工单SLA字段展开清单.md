# 工单 SLA 字段展开清单

## 业务域
- 工单 / SLA

## 目标
- 将 `ticket` 主表中的 `sla_*` 通配符展开为明确字段，支持首响、解决、暂停、状态与统计共用。

## 开发任务
- [x] 在 `ticket` 主表中展开 SLA 字段：首响截止、解决截止、实际首响、实际解决、SLA 状态、暂停累计时长、暂停开始时间。
- [x] 在 `3.4.4 SLA 与计时` 中补充 SLA 字段落主表的口径。
- [x] 保留 SLA 预警、违约动作、暂停计时与终止规则的既有行为说明。

## 验证记录
- [x] PRD 中不再使用 `sla_*` 通配符描述工单 SLA 核心字段。
- [x] `ticket` 主表可直接承载 SLA 计时与状态展示。
