# 安全风控与告警清单

## 业务域
- 认证与账号 / 平台安全

## 目标
- 将登录验证码、登录锁定、找回密码频次、敏感操作密码复核窗口、核心敏感数据静态加密、接口限流与安全告警中心的口径收敛为一套可执行规则，避免安全策略只停留在描述层。

## 开发任务
- [x] 在 `3.1.7 账号安全与自助管理` 中补充登录、注册、找回密码验证码/滑块校验，以及单账号失败锁定阈值与找回密码频次限制。
- [x] 在 `3.2.7 系统设置` 中补充全局安全策略、风控阈值与附件大小上限，保证验证码、锁定、限流和复核窗口可配置。
- [x] 在 `3.8 安全与屏蔽` 中补充核心敏感数据静态加密、敏感操作密码 step-up 窗口与安全告警中心口径。
- [x] 在 `5. 数据模型` 中补充 `security_alert` 数据模型，承载敏感告警的持久化与处理状态。
- [x] 在 `7. MVP 验收标准` 中补充登录风控、附件上传、高频消息/建工单与敏感操作告警的验收项。

## 验证记录
- [x] 登录、注册、找回密码的验证码策略已写入 PRD。
- [x] 单账号登录失败锁定、找回密码频次与咨询/附件限流口径已写入 PRD。
- [x] 敏感操作仅采用密码验证，且复用窗口已写入 PRD。
- [x] `security_alert` 已定义为仅管理端安全告警中心可见，不向客户或域管理员主动推送。
