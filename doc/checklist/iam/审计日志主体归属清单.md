# 审计日志主体归属清单

## 业务域
- IAM / 审计

## 目标
- 将 `audit_log` 的操作者关联从具体账号表收敛到统一身份主体，兼容员工、客户与系统三类审计来源。

## 开发任务
- [x] 在安全章节中明确客户触发的关键字屏蔽命中也写入 `audit_log`。
- [x] 在 `audit_log` 中采用 `operator_subject_id` 作为统一关联主体。
- [x] 通过 `operator_actor_type` 区分 `staff` / `customer` / `system`。
- [x] 复查 `audit_log` 口径，确认不再只绑定 `staff_account`。

## 验证记录
- [x] 客户内容触发的审计日志可追溯到对应统一身份主体。
- [x] 员工操作审计日志可追溯到对应统一身份主体。
- [x] 系统类审计日志可由 `operator_actor_type=system` 表达。
