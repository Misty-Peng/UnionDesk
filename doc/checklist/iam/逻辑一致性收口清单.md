# 逻辑一致性收口清单

- [x] 明确 `platform_admin` 仅可查看业务域客户/成员目录基础信息，不默认拥有域内容访问权。
- [x] 明确 `feedback/建议` 作为工单底座专用类型，MVP 状态流固定，不开放域管理员修改。
- [x] 明确 `ticket_reply.sender_type` 取值为 `customer` / `staff` / `system`。
- [x] 明确身份归并仅调整统一身份主体关系，不迁移历史工单、咨询和审计日志。
