# 在线咨询约束对齐清单

## 业务域
- 在线咨询

## 目标
- 将实时通信、排队体验、客服状态、消息类型、消息撤回、文件限制和评价流程补齐到 PRD 与数据模型中。

## 开发任务
- [x] 明确在线咨询采用 `WebSocket` 为主、HTTP API 为辅的通信方式。
- [x] 明确客服同时接待上限默认 `5`，支持按业务域配置。
- [x] 明确无客服在线时采用“留言 + 知识引导”的降级策略。
- [x] 明确活跃会话默认 `10` 分钟无新消息自动关闭，支持按业务域自定义。
- [x] 在数据模型中补充 `consultation_config` 与会话时间字段。
- [x] 明确排队编号、前方人数和等待时间的展示口径。
- [x] 明确客服状态支持 `在线 / 忙碌 / 离线`，并自动切换。
- [x] 明确 `consultation_message.type` 枚举范围与 `system` 语义。
- [x] 明确客户与客服均可撤回自己发送的消息。
- [x] 明确咨询文件传输限制复用全局附件策略。
- [x] 明确 `typing indicator` 不作为 MVP 能力。
- [x] 明确评价维度、邀请时机、过期与提醒规则。
- [x] 将默认评价窗口调整为 3 小时，到期前 30 分钟提醒一次。

## 验证记录
- [x] PRD 中已明确排队编号、前方人数和预估等待时间。
- [x] PRD 中已明确客服状态与接待上限规则。
- [x] PRD 中已明确消息类型、撤回和文件限制。
- [x] PRD 中已明确评价流程与 3 小时默认评价窗口。
- [x] 已修复 3.5.2 与验收 #31 的乱码，咨询评价窗口与提醒规则已统一为 3 小时 / 30 分钟。
- [x] 已将咨询满意度评价窗口统一为 3 小时，并与工单评价保持一致。
