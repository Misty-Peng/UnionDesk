# 通知中心持久化清单

## 业务域
- 通知中心

## 目标
- 将站内信、邮件、短信的发送记录与站内信收件箱状态持久化，避免通知中心只停留在事件描述层。

## 开发任务
- [x] 在 `3.9 通知中心` 中明确站内信使用 `inbox_message` 承载已读/未读与收件箱展示。
- [x] 在 `3.9 通知中心` 中明确发送记录、重试结果写入 `notification_log`。
- [x] 在 `5. 数据模型（核心实体）` 中补充 `notification_log` 与 `inbox_message`。
- [x] 复查通知中心口径，确认站内信、邮件、短信的职责边界清晰。

## 验证记录
- [x] 站内信已读/未读状态可从 `inbox_message` 持久化读取。
- [x] 通知发送失败后的重试轨迹可从 `notification_log` 追踪。
