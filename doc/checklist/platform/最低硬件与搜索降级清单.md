# 最低硬件与搜索降级清单

## 已完成

- [x] 明确单机生产最低硬件推荐配置：`4 vCPU / 16 GB RAM / 200 GB SSD`
- [x] 明确推荐配置：`8 vCPU / 32 GB RAM / 500 GB SSD`
- [x] 明确知识库搜索默认使用 Elasticsearch / OpenSearch
- [x] 明确低配仅允许开发 / 演示环境降级为 MySQL 全文检索

## 验证

- 单机资源预留需覆盖 Spring Boot、MySQL、Redis、Elasticsearch 和系统缓存
- 生产环境不默认使用 MySQL 全文检索
- 开发 / 演示环境可在资源不足时降级搜索方案
