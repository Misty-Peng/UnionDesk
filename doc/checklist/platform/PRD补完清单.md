# PRD补完清单

- [x] 浏览器兼容性：明确支持 Chrome / Edge / Firefox 最新两个主版本，以及 Safari 最新两个主版本，不支持 IE。
- [x] CSV 批量导入规范：明确 UTF-8 CSV 模板、必填列、校验规则、部分成功与错误报告策略。
- [x] XSS / CSRF 防护：补充输入净化、输出编码、CSRF Token 与来源校验口径。
- [x] 员工端 IP 访问控制：补充员工端 / 平台后台 IP 白名单策略与落库模型。
- [x] WebSocket 鉴权方式：补充 `ws_ticket` 短时一次性鉴权流程。
- [x] Slug 生成规则：补充知识库 `slug` 自动生成、唯一性和锁定规则。
- [x] 域软删除后的数据访问：补充历史工单、咨询、知识与审计的只读查询口径。
- [x] 咨询排队策略：补充严格 FIFO，默认不做 VIP / 紧急插队。
- [x] `ticket_history` 客户操作：补充统一主体记录，支持客户撤回等动作留痕。
- [x] `platform_admin` 授权边界：补充可授予 / 回收 `super_admin`，以及创建业务域后自动补授规则。

- [x] 本轮验收标准补充：工单满意度评价、安全告警处置、域软删除后数据可查、归档/导出任务可追踪、员工 CSV 导入的验收项已补齐。
- [x] ???????????????????????????????????????????????/?????????? CSV ???????????
- [x] 本轮验收标准已完成重新编号并合并重复项，工单满意度评价、安全告警处置、域软删除后数据可查、归档/导出任务可追踪、员工 CSV 导入均保留为可验收条目。
