# 设计一致性补全清单

## 已完成

- [x] 员工端与客户端密码复杂度按 `portal_type` 分别配置
- [x] `audit_log` 增加 `domain_id`，支持按业务域筛选
- [x] `ticket_reply` 增加 `created_at`，工单详情按时间线展示

## 验证

- 员工端与客户端可采用不同密码复杂度默认值
- 域内审计可按业务域过滤，平台级操作保留 `domain_id = NULL`
- 工单回复流可按创建时间排序展示
