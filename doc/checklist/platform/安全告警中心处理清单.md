# 安全告警中心处理清单

## 业务域
- 平台安全 / 风控

## 目标
- 明确安全告警中心的状态流、可处理角色、备注要求与批量处置能力。

## 开发任务
- [x] 在 `3.8` 中新增安全告警中心操作描述。
- [x] 明确告警状态流为 `待处理 → 已确认 → 已处置 / 已忽略`。
- [x] 明确仅 `super_admin` / `platform_admin` 可见和处置安全告警。
- [x] 明确 `已处置`、`已忽略` 需要填写备注，`已确认` 可选备注。
- [x] 明确安全告警支持按严重级别、类型、业务域和状态筛选。
- [x] 明确安全告警支持单条和批量处置。

## 验证记录
- [x] PRD 中已明确安全告警中心的状态流。
- [x] PRD 中已明确安全告警中心的可处理角色。
- [x] PRD 中已明确安全告警中心的筛选和批量处置能力。
- [x] `security_alert.status` 状态枚举已在数据模型中说明。
